for any prefilled defaults, like appliances, can we have a default svg icon to represent each?


---


we now need a way for architects and interior designers to generate a pdf of schedule on A4 or A3 to send to a client, vertical or horizontal. customise what info is shown. it needs version control and tracking in between revision. we need to store the different versions without making it too complicated. and maybe showing on the schedule in schedule what has changed somehow, so a contractor or client knows whats changed. 


---v2.0 features---



---


allow users to add, edit and delete fields in the material entry view, in some sort of editor or template editor field. 
please plan and come up with this new feature, im not sure how it will work and integrate into the current site

no gradients in materials

come up with a feature or button to automatically rename the current entries code, so that when a button is pressed, it will rename the code to something nice. it may find any unused codes, or maybe increment the highest code and add +1?
please plan and come up with this new feature, im not sure how it will work and integrate into the current site


allow users to add custom categories. sometimes users don't want to come up with the codes and categories, we should have a good set of defaults for them. and good default codes. also we may want a way to change the default code format, maybe in the settings. maybe its two letter like TM01 or TM-01 or TIM-01 etc. give a list of options. of course the user can override it.
please plan and come up with this new feature, im not sure how it will work and integrate into the current site



allow user to sort "assign to component"

entry title should be the full rendered name

rather than dropdown, make expandable, allow user to sort, give categories in the list to help organise the information.

settings:
in settings, give some options for a san serif title
allow the users to enter their own custom stages
make one more stage between studio and full width
what does IV mean on the settings button?
the icon looks like a sun, should be more of a gear?
a way to add the firms or users details. which will change the architects name on the page in header and footer. maybe later on (not now) we allow them to upload an image of their logo. 



Saved views as tabs — currently filters are per-session only; I can make them nameable and persist as tabs between Gallery/Table
Projects / Cost Schedule haven't got a Table mode yet — Library was the stated scope
The action bar and side panel could be made draggable / resizable